---
name: fg-plan-sequence-diagram
context: fg
description: "Render a sequence diagram for cross-module flows in the plan."
provides: [plan, sequence]
stages: [plan]
applies_to: [architectural]
---

# fg-plan-sequence-diagram

Use Mermaid `sequenceDiagram` syntax inside the data-model or api-contract artifact.
